function serie_Fourier(S_o,S_an,S_bn,max_n)

    fprintf('\n %.2f +2(',S_o);
    for n=1:max_n
        fprintf('+ %f*cos(%d*W_o*t)',S_an(n),n);
    end
    for n=1:max_n
        fprintf('+ %f*sin(%d*W_o*t)',S_bn(n),n);
    end
    fprintf(')');
end